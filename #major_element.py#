#!/usr/bin/env python

from sys import argv
from collections import Counter

input = open(argv[1])

for line in input:
    items = line.rstrip().split(',')
#    items = line.split(',')
    L = len(items)
    count = Counter(items).most_common(n)
    print count
#    none = True
#    for item in :
#        if items.count(item) > L/2:
#            print item
#            none = False
#            break
#    if none:
#        print 'None'
